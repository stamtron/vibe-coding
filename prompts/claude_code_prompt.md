# Claude Code Prompt (Era 6)

Run `claude` in the terminal, then type:

---

Read the marketing CSV in data/marketing_campaign.csv, create a comprehensive analysis with a 4-panel dashboard (spend vs revenue, ROAS, CTR, spend distribution), calculate key KPIs per channel, print a summary report, and save everything to plots/. Use pandas and matplotlib.

---

The key difference: Claude Code reads the CSV itself, writes the script, runs it, sees any errors, fixes them, and iterates — all without you leaving the terminal. You describe WHAT, it handles everything.
