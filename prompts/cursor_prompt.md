# Cursor Prompt (Era 5)

Open the project in Cursor, then use Cmd+K or the chat panel:

---

Look at data/marketing_campaign.csv and create a Python script that builds a 2x2 dashboard with:
- Panel 1: Spend vs Revenue grouped bar chart
- Panel 2: ROAS per channel with break-even line
- Panel 3: CTR horizontal bar chart
- Panel 4: Spend distribution by channel

Use a consistent color palette per channel. Save to plots/era6_channel_dashboard.png.

---

Then to iterate, select the plot code and Cmd+K:

"add value labels on top of the ROAS bars"
